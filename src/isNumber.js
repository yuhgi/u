/**
 * @description Check if `o` is Number.
 * @param {Any} o The obj to be checked.
 * @return {Boolean}
 * 
 * @author yuhgi
 * @version 0.0.1
 */
export default function(n){
    return typeof n === "number";
}