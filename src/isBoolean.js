/**
 * @description Check if `o` is Boolean.
 * @param {Any} o The obj to be checked.
 * @return {Boolean}
 * 
 * @author yuhgi
 * @version 0.0.1
 */

export default function(o){
    return typeof o === "boolean";
}