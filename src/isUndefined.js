/**
 * @description Check if `o` is undefined.
 * @param {Any} o The obj to be checked.
 * @return {Boolean}
 * 
 * @author yuhgi
 * @version 0.0.1
 */
export default function(o){
    return typeof o === "undefined";
}